---
#
# Ansible managed
#
dependency:
  name: galaxy
  options:
    role-file: requirements.yml
lint: |
  yamllint molecule/default/converge.yml
  ansible-lint molecule/default/converge.yml
driver:
  name: docker
platforms:
  - name: "mssql-${image:-centos}-${tag:-7}${TOX_ENVNAME}"
    image: "${namespace:-robertdebock}/${image:-centos}:${tag:-7}"
    command: /sbin/init
    volumes:
      - /sys/fs/cgroup:/sys/fs/cgroup:ro
    privileged: yes
    pre_build_image: yes
provisioner:
  name: ansible
  config_options:
    defaults:
      stdout_callback: yaml
      bin_ansible_callbacks: yes
verifier:
  name: ansible
scenario:
  name: sqlserver2019
